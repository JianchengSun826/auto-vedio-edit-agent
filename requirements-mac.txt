# Apple Silicon (M-chip) native dependencies
# Runs directly on macOS — no Docker needed
# Neural Engine acceleration via Apple MLX framework

mlx-whisper>=0.4.0
pyannote.audio>=3.1.0
torch>=2.2.0
anthropic>=0.40.0
gradio>=6.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pytest>=8.0.0
pytest-mock>=3.12.0
python-dotenv>=1.0.0
